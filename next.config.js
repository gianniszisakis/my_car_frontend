/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.carlogos.org",
      "avatars.githubusercontent.com",
      "icons.veryicon.com",
    ],
  },
};

module.exports = nextConfig;
